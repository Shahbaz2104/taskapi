export default function Dashboard() {
  return (
    <div>
      <h1 className="font-display text-3xl font-semibold">Control deck</h1>
      <p className="mt-2 text-sm text-ink-2">
        Tasks board arrives in Phase 3. You are authenticated — the silent
        refresh loop is live.
      </p>
    </div>
  );
}
